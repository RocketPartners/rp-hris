name: CD (Continuous Deployment)

on:
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types:
      - completed

permissions:
  contents: read
  id-token: write # Required for OIDC authentication (if using AWS/GCP/Azure)

jobs:
  # Check which apps were affected in the CI run
  check-affected:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    outputs:
      web-affected: ${{ steps.check-web.outputs.affected }}
      api-affected: ${{ steps.check-api.outputs.affected }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Derive appropriate SHAs for base and head for Nx affected commands
        uses: nrwl/nx-set-shas@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check if web was affected
        id: check-web
        run: |
          if npx nx show projects --affected --json | grep -q '"web"'; then
            echo "affected=true" >> $GITHUB_OUTPUT
            echo "✅ Web app was affected - will deploy"
          else
            echo "affected=false" >> $GITHUB_OUTPUT
            echo "⏭️ Web app was NOT affected - skipping deployment"
          fi

      - name: Check if api was affected
        id: check-api
        run: |
          if npx nx show projects --affected --json | grep -q '"api"'; then
            echo "affected=true" >> $GITHUB_OUTPUT
            echo "✅ API was affected - will deploy"
          else
            echo "affected=false" >> $GITHUB_OUTPUT
            echo "⏭️ API was NOT affected - skipping deployment"
          fi

  # Deploy Web App (only if affected)
  deploy-web:
    needs: check-affected
    if: needs.check-affected.outputs.web-affected == 'true'
    runs-on: ubuntu-latest
    environment:
      name: production-web
      url: https://your-web-app.com # TODO: Update with your actual URL
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        run: npx nx build web --configuration=production

      # TODO: Choose your deployment platform and uncomment the relevant section below

      # Option 1: Deploy to Vercel
      # - name: Deploy to Vercel
      #   env:
      #     VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      #     VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      #     VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      #   run: |
      #     npm install -g vercel
      #     vercel --prod --token=$VERCEL_TOKEN --yes
      #   working-directory: dist/apps/web

      # Option 2: Deploy to Netlify
      # - name: Deploy to Netlify
      #   uses: nwtgck/actions-netlify@v2
      #   with:
      #     publish-dir: './dist/apps/web'
      #     production-branch: main
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     deploy-message: "Deploy from GitHub Actions"
      #   env:
      #     NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      #     NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      # Option 3: Deploy to AWS S3 + CloudFront
      # - name: Configure AWS Credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: arn:aws:iam::ACCOUNT_ID:role/GitHubActionsRole
      #     aws-region: us-east-1
      #
      # - name: Deploy to S3
      #   run: |
      #     aws s3 sync dist/apps/web s3://your-bucket-name --delete
      #     aws cloudfront create-invalidation --distribution-id YOUR_DIST_ID --paths "/*"

      - name: Deployment completed
        run: echo "✅ Web app deployed successfully"

  # Deploy API (only if affected)
  deploy-api:
    needs: check-affected
    if: needs.check-affected.outputs.api-affected == 'true'
    runs-on: ubuntu-latest
    environment:
      name: production-api
      url: https://api.your-domain.com # TODO: Update with your actual URL
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build API
        run: npx nx build api --configuration=production

      # TODO: Choose your deployment platform and uncomment the relevant section below

      # Option 1: Deploy to Railway
      # - name: Deploy to Railway
      #   env:
      #     RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      #   run: |
      #     npm install -g @railway/cli
      #     railway up --service api
      #   working-directory: dist/apps/api

      # Option 2: Deploy to Render
      # - name: Deploy to Render
      #   env:
      #     RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      #     RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
      #   run: |
      #     curl -X POST "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
      #       -H "Authorization: Bearer $RENDER_API_KEY" \
      #       -H "Content-Type: application/json" \
      #       -d '{"clearCache": "clear"}'

      # Option 3: Deploy to AWS Elastic Beanstalk
      # - name: Configure AWS Credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: arn:aws:iam::ACCOUNT_ID:role/GitHubActionsRole
      #     aws-region: us-east-1
      #
      # - name: Deploy to Elastic Beanstalk
      #   run: |
      #     zip -r deploy.zip dist/apps/api
      #     aws elasticbeanstalk create-application-version \
      #       --application-name rp-hris-api \
      #       --version-label ${{ github.sha }} \
      #       --source-bundle S3Bucket="your-bucket",S3Key="deploy.zip"
      #     aws elasticbeanstalk update-environment \
      #       --application-name rp-hris-api \
      #       --environment-name production \
      #       --version-label ${{ github.sha }}

      # Option 4: Deploy to Heroku
      # - name: Deploy to Heroku
      #   uses: akhileshns/heroku-deploy@v3.13.15
      #   with:
      #     heroku_api_key: ${{secrets.HEROKU_API_KEY}}
      #     heroku_app_name: "your-api-app-name"
      #     heroku_email: "your-email@example.com"
      #     appdir: "dist/apps/api"

      # Option 5: Deploy to Docker/Container Registry
      # - name: Build and push Docker image
      #   run: |
      #     docker build -t your-registry/rp-hris-api:${{ github.sha }} -f apps/api/Dockerfile .
      #     docker push your-registry/rp-hris-api:${{ github.sha }}
      #
      # - name: Deploy to Kubernetes/ECS
      #   run: |
      #     # Update your deployment with the new image
      #     kubectl set image deployment/api api=your-registry/rp-hris-api:${{ github.sha }}

      - name: Deployment completed
        run: echo "✅ API deployed successfully"

  # Summary of deployments
  deployment-summary:
    needs: [check-affected, deploy-web, deploy-api]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Summary
        run: |
          echo "## Deployment Summary 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.check-affected.outputs.web-affected }}" == "true" ]; then
            if [ "${{ needs.deploy-web.result }}" == "success" ]; then
              echo "✅ **Web App**: Deployed successfully" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ **Web App**: Deployment failed" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "⏭️ **Web App**: Skipped (no changes)" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "${{ needs.check-affected.outputs.api-affected }}" == "true" ]; then
            if [ "${{ needs.deploy-api.result }}" == "success" ]; then
              echo "✅ **API**: Deployed successfully" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ **API**: Deployment failed" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "⏭️ **API**: Skipped (no changes)" >> $GITHUB_STEP_SUMMARY
          fi
